# Agent 工作台

> **修改注意：** 本文件由模板项目 [overwatchd/TemplateABC](https://github.com/overwatchd/TemplateABC) 统一管理。写入此文件的规则必须能跨项目复用，是所有 Agent 工作区的通用规则。项目特有内容写入 `README.md`。

## 文件分工

| 文件 | 作用 | 管理方式 |
|------|------|----------|
| `CLAUDE.md` | Agent 入口，强制指向 AGENTS.md | 模板 |
| 根 `AGENTS.md` | 跨项目通用规则（本文件） | 模板 |
| `README.md` | 项目描述、Agent 索引、目录结构 | 项目自有 |
| `<workspace>/AGENTS.md` | 工作区专属规则 | 项目自有 |

Agent 以仓库根目录为工作目录。**操作子目录前必须完整读取该目录的 AGENTS.md**，子目录规则与根规则同等效力。

---

## 全局规则

### 规则管理

- 所有规则必须写入 `AGENTS.md`，**禁止**写入入口文件、README 或其他文件。
- **禁止**在 `AGENTS.md` 中提及特定 Agent 实现（如 Claude、Copilot、Gemini）。规则面向所有 Agent，使用通用表述。

### 临时目录

- 临时文件、中间产物、检出的外部仓库，**必须**放入 `tmp/`（已 gitignored）。

### 跨设备兼容

- **禁止**硬编码绝对路径。使用相对路径、环境变量或可配置变量代替。

### 网络访问

- 访问网络受限时，先尝试本机 `127.0.0.1:10808` 代理。

### Skill 安装

- Skill 优先安装到 user 层。除非用户明确要求，否则不做项目级安装。
- 安装前与用户确认安装层级和目标范围。

### 并行工作

| 方式 | 适合场景 |
|------|----------|
| 多会话：不同终端各自操作不同子目录 | 日常并行 |
| git worktree：为每个 Agent 创建独立 worktree | 长期并行、需同时修改同一文件 |

### 版本控制

- 构建产物（node_modules、.env、编译输出等）统一在 `.gitignore` 排除。
- Agent 工作区相互独立，禁止跨工作区引用代码或配置。
- 提交记录文档时只保留最终有效内容，删除试错、回退等过程描述。

### 语言与快捷指令

- 对话和文档使用中文。
- 「提交」→ `git add -A` → `git commit -m "<自动生成消息>"` → `git push`

---

→ [README.md](README.md) — 本项目 Agent 索引、目录结构与项目描述
