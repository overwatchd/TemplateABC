# Agent 工作台规则

## 规则与入口

- 本仓库是多个独立子工作区的共同入口，职责与目录索引见 README。
- 在仓库根目录开展工作；操作子目录前完整读取沿途存在的 `AGENTS.md`。
- 根规则只保存跨工作区通用约定；工作区专属规则写入对应 `AGENTS.md`。
- 行为规则统一写入 `AGENTS.md`，入口文件只做导航，其他文档保存说明、资料与事实。
- 规则使用通用表述，不绑定特定 Agent 实现；根规则通过模板子模块统一同步。

## 工作区边界

- 各工作区独立维护代码和配置，可通过文档链接引用其他工作区的事实，不复制同一份配置或状态作为第二个真源。
- 稳定知识、设备事实、任务状态与实例运行数据分别记录；历史观测注明日期，不作为当前在线、已部署或操作授权的证明。
- 个人密钥、令牌、登录态、运行数据库和本机覆盖配置不进入仓库；留在用户目录或已忽略的 `local/`。
- 临时文件、下载包、中间产物、临时检出的外部仓库统一放在根目录 `tmp/`。
- 明确作为项目依赖的独立仓库可使用 Git 子模块，保留其独立历史并锁定提交；不复制多份实现。

## 跨设备与执行

- 可复用脚本和配置使用参数、环境变量、用户目录或相对路径，禁止硬编码设备绝对路径。
- 设备专属路径与端点可作为带来源的事实登记在所属工作区，不散布到通用执行脚本。
- 每台设备独立克隆 Git 仓库；不通过文件同步工具同步 `.git` 或运行中的应用数据库。
- 保留现有用户文件；安装、覆盖配置和服务变更按用户授权执行，已有明确授权不重复询问。
- 新增或修改执行工具时区分预览与写入、注明平台与验证范围；仓库整理不等于线上变更。
- 访问网络受限时，先尝试本机 `127.0.0.1:10808` 代理。
- Skill 优先安装到 user 层；安装前确认层级与目标范围，已有明确授权不重复询问。

## 维护与版本控制

- 对话和文档使用中文；操作流水账留在 Git，文档保持最终有效结论。必要的核验快照注明日期和证据范围。
- 调整目录时修复内部链接、脚本入口和子模块路径，检查现有本机引用是否受影响。
- 依赖、构建产物、日志、下载与私有配置由 `.gitignore` 排除。
- 并行修改同一批文件时使用独立 worktree。
- 「提交」表示依次执行 `git add -A`、`git commit -m "<自动生成消息>"`、`git push`。
